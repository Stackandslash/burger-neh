<h1>Burgers!</h1>

<h2>Burgers you can eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you have eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="br">Burger Name:</label> <!--Not sure if the label helps or not, but it's good for accessability anyway.-->
    <input type="text" id="br" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>
